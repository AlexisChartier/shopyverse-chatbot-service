apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chatbot-service.fullname" . }}-secrets
  labels:
    {{- include "chatbot-service.labels" . | nindent 4 }}
type: Opaque
# NOTE: Create this secret manually or via your secret management system
# kubectl create secret generic chatbot-service-secrets \
#   --from-literal=hf-access-token=<token> \
#   --from-literal=api-key=<key> \
#   --from-literal=db-password=<password> \
#   -n chatbot-dev
