apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "chatbot-service.serviceAccountName" . }}
  labels:
    {{- include "chatbot-service.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
